# 🛡️ MKG-app 開発環境安定化ガイド

## 📋 概要

このガイドは、MKG-appの開発環境で発生した**構文エラー多発・ファイル破損問題**の再発を防ぐためのものです。

## 🚨 発生した問題の原因

### 1. **自動コミット機能の危険性**
- **問題**: 1時間ごとに`git add . && git commit && git push`が自動実行
- **影響**: 未完成・エラー含むコードの自動プッシュ
- **対策**: ✅ 自動コミット機能を無効化済み

### 2. **複数プロセス競合**
- **問題**: 最大8つのNext.jsサーバーが同時稼働
- **影響**: メモリ枯渇・ファイルロック・構文エラー誤報
- **対策**: ✅ プロセス管理システム導入済み

### 3. **キャッシュ競合**
- **問題**: `.next`フォルダへの同時アクセス
- **影響**: コンパイルエラー・ホットリロード障害
- **対策**: ✅ キャッシュクリア機能追加済み

## 🛠️ 導入した対策

### ✅ 1. 自動コミット機能の安全な無効化
```bash
# 無効化されたファイル
- auto_commit.bat.disabled
- auto_commit_simple.bat.disabled
- git_auto_commit_task.xml.disabled
```

### ✅ 2. プロセス管理システム
```bash
# 利用方法
dev-process-manager.bat
```
**機能:**
- 現在のプロセス確認
- 全プロセス終了
- 単一サーバー起動
- ポート状況確認
- キャッシュクリア

### ✅ 3. 安定化npm scripts
```json
{
  "dev:stable": "next dev --port 3000",
  "dev:clean": "npm run clean && npm run dev:stable",
  "process:kill": "taskkill /f /im node.exe",
  "process:check": "netstat -ano | findstr :300"
}
```

### ✅ 4. 開発環境設定
- `.env.development.stable`: メモリ制限・ポート固定・最適化設定

## 📝 開発開始前チェックリスト

### 🔄 **毎回必須の確認事項**

- [ ] 1. **プロセス状況確認**
  ```bash
  npm run process:check
  ```

- [ ] 2. **既存プロセス終了**（複数ある場合）
  ```bash
  npm run process:kill
  ```

- [ ] 3. **キャッシュクリア**
  ```bash
  npm run clean
  ```

- [ ] 4. **単一サーバー起動**
  ```bash
  npm run dev:stable
  # または
  npm run dev:clean
  ```

### 🚫 **絶対に避けること**

- [ ] ❌ 自動コミット機能の再有効化
- [ ] ❌ 複数のnpm run devの同時実行
- [ ] ❌ エラー状態でのgit commit
- [ ] ❌ .nextフォルダの手動編集

## 🆘 問題発生時の対処法

### **構文エラーが表示された場合**
1. **実際のエラーかを確認**
   ```bash
   npm run process:check
   ```

2. **プロセス整理**
   ```bash
   npm run process:kill
   npm run clean
   npm run dev:stable
   ```

3. **それでも解決しない場合**
   ```bash
   # 管理ツールを使用
   dev-process-manager.bat
   ```

### **ファイルが壊れた場合**
1. **Gitから復元**
   ```bash
   git status
   git restore pages/index.js
   ```

2. **クリーン起動**
   ```bash
   npm run dev:clean
   ```

## 📈 運用ルール

### **コミット時のルール**
1. **必ず動作確認後にコミット**
2. **エラーがある場合はコミット前に修正**
3. **自動コミットは絶対に使用禁止**

### **開発終了時のルール**
1. **全プロセス終了**
   ```bash
   npm run process:kill
   ```

2. **作業内容のコミット**（手動）
   ```bash
   git add .
   git commit -m "具体的な変更内容"
   git push
   ```

## 💻 Claude Codeクラッシュ対策

### 🚨 **Claude Code停止の原因と対処法**

#### **主要原因: メモリ不足**
- **問題**: 大量のアプリが同時実行されメモリ枯渇
- **症状**: プロジェクトクリーンアップ中にClaude Codeがクラッシュ

#### **📋 対処手順**

##### **immediate対応**
```bash
# PC再起動（最も確実）
shutdown /r /t 0
```

##### **根本対策: スタートアップアプリ無効化**
以下をタスクマネージャー > スタートアップで**無効**に設定:

**最重要**:
- Steam
- Slack (com.squirrel.slack.slack)
- Adobe Creative Cloud系
  - CCXProcess
  - AdobeBridge
  - Adobe Acrobat Synchronizer
  - AdobeGCInvoker-1.0
  - AdobeAAMUpdater-1.0

**その他重いアプリ**:
- 5KPlayer
- Luminar Neo
- MicrosoftEdgeAutoLaunch

##### **運用ルール**
- **✅ OK**: LINEWORKSなど軽量アプリはメモ帳代わりなら問題なし
- **❌ NG**: Steam、Slack、Creative Cloud等の重いアプリ
- **推奨**: プログラミング時はVS Code + ブラウザ程度に限定

#### **🔍 監視方法**
```bash
# メモリ使用量確認
tasklist | findstr /i "node\|npm\|steam\|slack\|adobe"
```

## 🔧 緊急時連絡先

**プロセス管理ツール**: `dev-process-manager.bat`
**設定ファイル**: `.env.development.stable`
**本ガイド**: `DEVELOPMENT_STABILITY_GUIDE.md`

---

**⚠️ このガイドを遵守することで、安定した開発環境を維持できます。**